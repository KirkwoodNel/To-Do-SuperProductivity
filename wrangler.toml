name = "sp-todo-sync"
main = "worker.js"
compatibility_date = "2026-01-01"

# Existing namespace from earlier work (id confirmed 2026-09-13).
# Binding name is SP_KV — not the old WebDAV SP_SYNC_KV.
kv_namespaces = [
  { binding = "SP_KV", id = "084c7fc8b473467da90bc3001fbbd95f" }
]

# Secrets (Cloudflare dashboard → Worker → Settings → Variables and Secrets):
#   DEBUG_SECRET
#   DROPBOX_APP_KEY
#   DROPBOX_APP_SECRET
# Optional:
#   DROPBOX_REFRESH_TOKEN   (also stored in KV after /auth/callback)
